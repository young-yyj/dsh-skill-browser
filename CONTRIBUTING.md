# 参与贡献

感谢参与 DSH Skill Browser。

## 开发流程

1. Fork 仓库并从 `main` 创建功能分支。
2. 使用 Node.js 22 或更高版本运行 `npm ci`。
3. 修改功能时同步增加或更新测试。
4. 提交前运行：

```powershell
npm run verify
npm pack --dry-run
```

5. 确认变更没有包含令牌、Cookie、用户目录、`.env`、`.npmrc`、私钥或 DSH 凭据。

## 发布 Release

每个插件版本对应一个 `v<版本号>` 标签和 GitHub Release。先更新版本并运行 `npm ci`、`npm run verify`，再用 `npm pack --pack-destination <临时目录>` 生成 `.tgz`；检查包内容，确认不含配置、凭据和个人目录。

为 `.tgz` 生成 SHA256 校验文件 `SHA256SUMS`，每行格式为 `<64位十六进制哈希><两个空格><包文件名>`。将这两个文件作为 Release 附件上传，源码归档不能代替已构建的 `.tgz`。发布说明应包含兼容的 DSH 版本、安装链接、主要变化、验证范围和已知限制。

Release 先保存为草稿，下载附件复核校验值和包内容后再发布。发布后的版本号、标签和附件不覆盖；后续修复递增插件版本。同步 README 的推荐版本和下载链接，保留旧 Release 供回退。

## 提交信息

默认采用中文“一句话介绍 + 多行修改摘要”格式：

```text
完善技能目录刷新机制

- 增加目录变化监听
- 补充异常与生命周期测试
```

## Pull Request

Pull Request 应说明变更目的、主要修改和验证结果。涉及安全边界、文件读取、HTML 渲染或新接口时，请明确解释风险与防护措施。

本项目当前定位为只读浏览器。新增写入、安装、删除或执行能力必须先单独讨论权限模型与回滚方案。
