# 安全策略

## 支持范围

安全修复优先应用于当前主分支和最新发布版本。早期提交及未经支持的 DSH 版本不保证单独维护。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口，并提供：

- 受影响版本与运行环境。
- 可复现步骤或最小示例。
- 潜在影响。
- 已知的缓解方式。

请勿在公开 Issue、讨论区或 Pull Request 中披露尚未修复的漏洞、凭据或个人数据。维护者确认问题并准备修复后，会协调披露时间。

## 安全原则

- 不提交访问令牌、Cookie、`.env`、`.npmrc`、私钥或 DSH 凭据文件。
- 新增接口默认只读，并维持回环地址和同源限制。
- 技能内容始终作为不可信文本处理，不执行技能正文中的 HTML、脚本或命令。
